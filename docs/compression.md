# Compression des médias

## Images

Les images sont compressées automatiquement à l'upload.

```
Original ──▶ Resize ──▶ Compression ──▶ Thumbnail
  (JPEG)      (max dim)   (qualité 80%)   (200x200)
```

## Vidéos

Les vidéos ne sont pas compressées côté serveur pour le moment.
